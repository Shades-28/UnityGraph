# Task: add an XML summary to EnemyAI listing where it's used

Add an XML `<summary>` doc comment to the `EnemyAI` class that lists the
scene(s) and prefab(s) that have EnemyAI attached. This helps future
maintainers find callers. Use the actual asset names from the project.
