# Task: add a Jump method to PlayerController

Add a public `Jump()` method that applies an upward force equal to
`_jumpForce` on `_rigidbody` using `ForceMode.Impulse`. Don't call it from
anywhere — just define the method.
