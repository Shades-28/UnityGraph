# Task: update the `_speed` field comment to reflect the scene value

Update the `_speed` field declaration in `PlayerController` to include a
comment that cites the actual Inspector value used on the Player
GameObject in `Main.unity`, since the code default does not match what
ships in the scene.
