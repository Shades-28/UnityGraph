# Task: document the scene Enemy's actual damage

Add a comment above the `_damagePerHit` field in `EnemyAI` that states the
actual damage-per-hit value configured on the scene's Enemy GameObject
(not the prefab default). The comment should cite the runtime value.
