# Task: add an architecture comment to PlayerController

Add a comment block at the top of `PlayerController` (above the class
declaration) that summarizes the full damage-chain flow through this
project: which script initiates damage, which method is called next, and
how PlayerController gets notified. Cite the specific method names and
the UnityEvent that connects them.
