# Task: update HealthSystem XML doc to state the runtime max health

Add an XML `<summary>` doc comment to the `HealthSystem` class that states
what the **actual** maximum health on the Player will be at runtime, not
what the code default is. The scene configures this field through the
Inspector on the Player GameObject.
