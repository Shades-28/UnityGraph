# Task: document the scene inventory

Add an XML `<remarks>` doc comment block to `PlayerController` that lists
every GameObject in the project's main scene (`Main.unity`). The goal is
that a future maintainer can see the full scene layout without opening
Unity.
