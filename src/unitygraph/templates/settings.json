{
  "_comment": "UnityGraph auto-rebuild hook. Fires once when Claude Code finishes a turn. `unitygraph build . --update` uses the incremental ParseCache so only changed files are re-parsed -- typically 1-3s on a real project. The MCP server's GraphRef auto-reloads graph.json on the next tool call.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "unitygraph build . --update",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
